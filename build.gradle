plugins {
  id "org.sonarqube" version "2.6.2" apply false
} 

subprojects { subproject ->
  apply plugin: 'java'
  apply plugin: 'org.sonarqube'
  apply plugin: 'jacoco'

  repositories {
    jcenter()
  }
}
/*
sonarqube {
  properties {
    property "sonar.projectKey", "s0mark_base"
    property "sonar.organization", "retelab"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}*/